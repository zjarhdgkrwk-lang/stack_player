package com.stack.domain.usecase.playback

import javax.inject.Inject

class AddNextUseCase @Inject constructor() {
    suspend operator fun invoke(track: com.stack.domain.model.Track) {
        TODO("Not yet implemented")
    }
}
